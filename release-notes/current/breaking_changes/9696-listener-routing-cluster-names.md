Inline backend and request-mirror clusters now use listener-specific names when a listener selects a different Service or Endpoint routing mode from the route/Gateway policy. EnvoyPatchPolicy resources and Extension Servers that select the previous cluster or endpoint names, or refer to them in route actions, must use the generated names for each affected listener. Shared backend clusters keep their existing naming scheme.
