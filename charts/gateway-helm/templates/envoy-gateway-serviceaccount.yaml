apiVersion: v1
kind: ServiceAccount
automountServiceAccountToken: false
metadata:
  name: envoy-gateway
  namespace: {{ include "eg.namespace" . }}
  labels:
  {{- include "eg.labels" . | nindent 4 }}
