global:
  imageRegistry: "docker.io"

deployment:
  envoyGateway:
    image:
      repository: "envoyproxy/gateway-dev"
      tag: "latest"
    imagePullPolicy: Always

certgen:
  job:
    args:
    - --overwrite
