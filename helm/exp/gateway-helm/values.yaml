deployment:
  envoyGateway:
    imagePullSecret: artifactory-reader
    image:
      repository: geocomply.jfrog.io/apps-idp-gateway-docker-local/envoyproxy/gateway-dev
      tag: b25b21e@sha256:9eb95d081296c1dfb72b5e172ad9682885781fe1edc6400430001cc66f5e0f49
    imagePullPolicy: Always
    resources:
      limits:
        cpu: 1000m
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 512Mi
  kubeRbacProxy:
    image:
      repository: gcr.io/kubebuilder/kube-rbac-proxy
      tag: v0.11.0
    resources:
      limits:
        cpu: 50m
        memory: 56Mi
      requests:
        cpu: 50m
        memory: 56Mi
  ports:
  - name: grpc
    port: 18000
    targetPort: 18000
  replicas: 1
rootNamespace: envoy-gateway-system
config:
  envoyGateway:
    gateway:
      controllerName: gateway.envoyproxy.io/gatewayclass-controller
    provider:
      type: Kubernetes
envoyGatewayMetricsService:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: https

createNamespace: false

kubernetesClusterDomain: cluster.local
