Removed the `watchable_depth` metric. It read the length of the subscription channel, which `watchable.Map.Subscribe` returns unbuffered, so it always reported 0. The bundled Grafana dashboards now build their Namespace and Runner variables from `watchable_subscribe_total`, and the depth panel is gone; any alert or dashboard of your own that queries `watchable_depth` should be removed or repointed.
