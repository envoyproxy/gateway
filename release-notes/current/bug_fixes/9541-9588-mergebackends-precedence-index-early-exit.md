Fixed the MergeBackends precedence-resolution indexes for BackendTrafficPolicy (`RoutingType`, cluster-scoped settings eligibility, `LoadBalancer` ConsistentHash) and ClientTrafficPolicy (cluster-scoped settings eligibility): an accepted policy that set nothing at its target (e.g. a route-rule/route-targeted BackendTrafficPolicy with `mergeType` unset, or a Gateway/Listener/ListenerSet-targeted policy with no relevant field set) previously skipped claiming its precedence slot entirely, letting a younger conflicting policy silently win, or letting the policy incorrectly inherit its parent's settings instead of pinning to its own.
