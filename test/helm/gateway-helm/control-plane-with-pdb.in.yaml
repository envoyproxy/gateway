global:
  imageRegistry: "docker.io"

deployment:
  envoyGateway:
    image:
      repository: "envoyproxy/gateway-dev"
      tag: "latest"
    imagePullPolicy: Always

podDisruptionBudget:
  minAvailable: 1
  maxUnavailable: 1
