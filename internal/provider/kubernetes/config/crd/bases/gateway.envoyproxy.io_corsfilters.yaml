---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: corsfilters.gateway.envoyproxy.io
spec:
  group: gateway.envoyproxy.io
  names:
    kind: CorsFilter
    listKind: CorsFilterList
    plural: corsfilters
    singular: corsfilter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the CorsFilter type.
            properties:
              allow_headers:
                description: AllowHeaders defines a list of HTTP headers that will
                  be allowed to do CORS requests. An HTTP header is allowed if it
                  matches either an item in this list or the special "*" wildcard.
                type: string
              allow_methods:
                description: AllowMethods defines a list of HTTP methods that will
                  be allowed to do CORS requests. An HTTP method is allowed if it
                  matches either an item in this list or the special "*" wildcard.
                type: string
              allow_origin_string_match:
                description: AllowOriginStringMatch defines a list of origins that
                  will be allowed to do CORS requests. An origin is allowed if it
                  matches either an item in this list or the special "*" wildcard.
                items:
                  description: StringMatch defines a string match.
                  properties:
                    prefix:
                      description: Prefix defines a string prefix match.
                      type: string
                  type: object
                type: array
              expose_headers:
                description: ExposeHeaders defines a list of HTTP headers that will
                  be exposed as part of the CORS response.
                type: string
              max_age:
                description: MaxAge defines the duration that the results of a preflight
                  request can be cached.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
