name: Return Action on PR Comment

on:
  issue_comment:
    types: [created]

permissions:
  contents: read

jobs:
  command:
    if: |
      ${{
         github.event.issue.pull_request
         && github.repository == 'envoyproxy/gateway'
         && github.actor != 'repokitteh-read-only[bot]'
         && github.actor != 'dependabot[bot]'
      }}
    name: Retest
    runs-on: ubuntu-22.04
    permissions:
      pull-requests: write
      actions: write
    steps:
    - uses: envoyproxy/toolshed/gh-actions/github/command@0c093e23192533019cf131e0016adb0ac73adc97  # actions-v0.2.18
      name: Parse command from comment
      id: command
      with:
        text: ${{ github.event.comment.body }}
        matching: >-
          ^/(retest)

    # retest
    - uses: envoyproxy/toolshed/gh-actions/retest@0c093e23192533019cf131e0016adb0ac73adc97  # actions-v0.2.20
      if: ${{ steps.command.outputs.command == 'retest' }}
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        comment-id: ${{ github.event.comment.id }}
        pr-url: ${{ github.event.issue.pull_request.url }}
        app-owner: "github-actions"
        args: ${{ steps.command.outputs.args }}
