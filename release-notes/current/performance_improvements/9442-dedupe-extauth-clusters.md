Deduplicated external authorization (`SecurityPolicy` extAuth) upstream clusters by content hash, so multiple SecurityPolicies targeting the same ext auth backend now share a single Envoy cluster instead of generating one cluster per policy. This significantly reduces Envoy memory (duplicate upstream TLS contexts and cluster-stat cardinality) on gateways with a large number of extAuth policies.
