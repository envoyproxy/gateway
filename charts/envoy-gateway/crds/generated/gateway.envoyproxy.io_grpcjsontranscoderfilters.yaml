---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: grpcjsontranscoderfilters.gateway.envoyproxy.io
spec:
  group: gateway.envoyproxy.io
  names:
    kind: GrpcJSONTranscoderFilter
    listKind: GrpcJSONTranscoderFilterList
    plural: grpcjsontranscoderfilters
    singular: grpcjsontranscoderfilter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the GrpcJSONTranscoderFilter
              type.
            properties:
              auto_mapping:
                description: AutoMapping is a flag that indicates whether the filter
                  should automatically map the incoming request to the appropriate
                  method in the proto descriptor.
                type: boolean
              print_options:
                description: PrintOptions is a set of options that controls how the
                  filter generates the JSON response.
                properties:
                  add_whitespace:
                    description: AddWhitespace is a flag that indicates whether the
                      filter should add whitespace to the JSON response.
                    type: boolean
                  always_print_enums_as_ints:
                    description: AlwaysPrintEnumsAsInts is a flag that indicates whether
                      the filter should always print enums as ints in the JSON response.
                    type: boolean
                  always_print_primitive_fields:
                    description: AlwaysPrintPrimitiveFields is a flag that indicates
                      whether the filter should always print primitive fields in the
                      JSON response.
                    type: boolean
                  preserve_proto_field_names:
                    description: PreserveProtoFieldNames is a flag that indicates
                      whether the filter should preserve proto field names in the
                      JSON response.
                    type: boolean
                required:
                - add_whitespace
                - always_print_enums_as_ints
                - always_print_primitive_fields
                - preserve_proto_field_names
                type: object
              proto_descriptor_bin:
                description: ProtoDescriptorBin is the base64 encoded binary representation
                  of the proto descriptor.
                type: string
              services:
                items:
                  type: string
                type: array
            required:
            - auto_mapping
            - proto_descriptor_bin
            - services
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
