Fixed the ordering of HTTP filters when a listener has many filters of one type. The index within a type was added to the type's base order, so a listener with 38 or more Lua filters sorted the last of them after filters the ordering does not recognize, and at 88 they interleaved with ext_proc. The index is now a separate sort key, so a type's filters always stay within their own position in the chain.
