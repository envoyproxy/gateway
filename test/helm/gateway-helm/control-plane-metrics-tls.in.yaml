global:
  images:
    envoyGateway:
      image: "docker.io/envoyproxy/gateway-dev:latest"
      pullPolicy: Always
metrics:
  tls:
    enable: true
    secretRef:
      name: envoy-gateway-metrics-cert
