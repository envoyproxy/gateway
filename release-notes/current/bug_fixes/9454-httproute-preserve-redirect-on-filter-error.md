Fixed an HTTPRoute rule whose filters set a DirectResponse or Redirect (e.g. `RequestRedirect`) being overwritten with a `500` DirectResponse when another filter in the same rule fails to process. The route now preserves the filter-configured DirectResponse/Redirect, matching the existing GRPCRoute behavior. The filter error is still surfaced in the HTTPRoute `Accepted` status condition.
