deployment:
  envoyGateway:
    imagePullSecret: artifactory-reader
    image:
      # repository: geocomply.jfrog.io/apps-idp-gateway-docker-local/envoyproxy/gateway-dev
      # tag: latest
      repository: docker.io/envoyproxy/gateway-dev
      tag: 18789a1@sha256:ed89b8f6c16404a62fa9f29970bb5ee8764fe2ffeb98e8599f4476f7e9933be6
    imagePullPolicy: Always
    resources:
      limits:
        cpu: 1000m
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 512Mi
  kubeRbacProxy:
    image:
      repository: gcr.io/kubebuilder/kube-rbac-proxy
      tag: v0.11.0
    resources:
      limits:
        cpu: 50m
        memory: 56Mi
      requests:
        cpu: 50m
        memory: 56Mi
  ports:
  - name: grpc
    port: 18000
    targetPort: 18000
  - name: ratelimit
    port: 18001
    targetPort: 18001
  replicas: 1
rootNamespace: envoy-gateway-system
config:
  envoyGateway:
    gateway:
      controllerName: gateway.envoyproxy.io/gatewayclass-controller
    provider:
      type: Kubernetes
    rateLimit:
      backend:
        type: Redis
        redis:
          url: engine-dev-replication-unauthenticated.wccceb.ng.0001.use1.cache.amazonaws.com:6379
          
envoyGatewayMetricsService:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: https

createNamespace: false

kubernetesClusterDomain: cluster.local
