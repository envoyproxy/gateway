Added `metadata` to `BackendTrafficPolicy.spec.loadBalancer.endpointOverride.extractFrom`, allowing the override endpoint to be read from per-request dynamic metadata instead of a request header. Unlike a header, dynamic metadata cannot be set by a downstream client and is not forwarded to the backend, so it is the recommended source when the endpoint is selected in-proxy by an external processor, Lua or Wasm.
