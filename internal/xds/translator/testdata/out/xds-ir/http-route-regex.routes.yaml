- ignorePortInHostMatching: true
  name: first-listener
  virtualHosts:
  - domains:
    - '*'
    name: first-listener/*
    routes:
    - match:
        headers:
        - name: re_header
          stringMatch:
            safeRegex:
              googleRe2: {}
              regex: .*
        queryParameters:
        - name: re_query
          stringMatch:
            safeRegex:
              googleRe2: {}
              regex: .*
        safeRegex:
          googleRe2: {}
          regex: /v1/.*
      name: regex-route
      route:
        cluster: regex-route-dest
        upgradeConfigs:
        - upgradeType: websocket
