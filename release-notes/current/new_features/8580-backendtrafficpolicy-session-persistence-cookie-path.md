Added a `sessionPersistence.cookie.path` field to `BackendTrafficPolicy`, allowing the session persistence cookie `Path` attribute to be overridden. This keeps sticky sessions working when the request path is rewritten by an upstream proxy before it reaches Envoy Gateway, which would otherwise cause the cookie `Path` derived from the matched route to not match the client-facing URL.
